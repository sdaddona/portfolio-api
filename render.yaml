services:
  - type: web
    name: portfolio-analyzer
    runtime: docker
    plan: free
    envVars:
      - key: FRED_API_KEY
        value: ""   # opzionale: metti qui la tua API key FRED se vuoi Sharpe con RF reale
    autoDeploy: true
